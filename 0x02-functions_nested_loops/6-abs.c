#include "main.h"
/**
 * _abs - compute the absolute value of a number
 * @num: the number to compute its absolute
 *
 * Return: the absolute value of the number
 */
int _abs(int num)
{
	return ((num < 0) ? -num : num);
}
